# Segredos locais (ANTHROPIC_API_KEY...)
.env

# Binários Go
*.exe
/bin/

# Base de cartas local (regenerável via `go run ./cmd/import <setID>`)
/data/*
# Banco de efeitos é curado (LLM + revisão humana) — commitado
!/data/effects.json

.vscode/

# Frontend (web/dist é commitado para o go:embed funcionar sem Node)
/web/node_modules/
.playwright-mcp